"""Optional persistence backends (PostgreSQL).

Most clients can run on the file backend forever. PostgreSQL is provided as a
template for clients that need cross-process session sharing or analytics.
"""
